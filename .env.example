# =============================================================================
# a-share-heatmap 环境变量示例
# 复制为 .env 后按需填写：cp .env.example .env
# 注意：.env 已被 .gitignore 忽略，切勿提交真实凭据
# =============================================================================

# ---- 主站（Next.js，均为可选） ----

# 东财云自选同步服务的地址（浏览器直接访问，需服务已开启 CORS）
# 缺省为 http://localhost:8787；只有同步服务不在本机时才需要设置
# NEXT_PUBLIC_EM_API_URL=http://192.168.1.10:8787

# ---- east-money-api-node（独立 Fastify 服务，位于 east-money-api-node/） ----

# 东方财富 Cookie：登录 quote.eastmoney.com 后，F12 → 网络请求里复制 Cookie 整串
# 支持直接粘贴整串，也支持填一个包含 Cookie 的本地文件路径
# 不配置时行情接口仍可用，但自选股分组/同步接口返回空
EASTMONEY_COOKIE=

# 东方财富 AppKey（部分 myfavor 接口需要；一般可从 Cookie/请求参数中获取，可留空）
EASTMONEY_APPKEY=

# Fastify 服务监听端口与地址
PORT=8787
HOST=0.0.0.0
